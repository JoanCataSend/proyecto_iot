<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/navbar"
    android:layout_width="match_parent"
    android:layout_height="64dp"
    android:layout_gravity="bottom"
    android:orientation="horizontal"
    android:gravity="center"
    android:elevation="12dp"
    android:paddingHorizontal="12dp"
    android:paddingVertical="6dp"
    android:background="@color/nav_background_light">

    <!-- HOME -->
    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:layout_weight="1"
        android:orientation="vertical"
        android:gravity="center_horizontal">

        <View
            android:id="@+id/indicator_home"
            android:layout_width="24dp"
            android:layout_height="3dp"
            android:layout_marginBottom="4dp"
            android:background="@drawable/navbar_indicator"
            android:visibility="invisible" />

        <ImageButton
            android:id="@+id/nav_home"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@drawable/ic_casa"
            android:padding="10dp"
            android:scaleType="centerInside"
            android:clickable="true"
            android:focusable="true"
            app:tint="@color/nav_icon_dark_inactive" />
    </LinearLayout>

    <!-- CAR -->
    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:layout_weight="1"
        android:orientation="vertical"
        android:gravity="center_horizontal">

        <View
            android:id="@+id/indicator_car"
            android:layout_width="24dp"
            android:layout_height="3dp"
            android:layout_marginBottom="4dp"
            android:background="@drawable/navbar_indicator"
            android:visibility="invisible" />

        <ImageButton
            android:id="@+id/nav_car"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@drawable/ic_coche"
            android:padding="10dp"
            android:scaleType="centerInside"
            android:clickable="true"
            android:focusable="true"
            app:tint="@color/nav_icon_dark_inactive" />
    </LinearLayout>

    <!-- NOTIFICATIONS -->
    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:layout_weight="1"
        android:orientation="vertical"
        android:gravity="center_horizontal">

        <View
            android:id="@+id/indicator_notifications"
            android:layout_width="24dp"
            android:layout_height="3dp"
            android:layout_marginBottom="4dp"
            android:background="@drawable/navbar_indicator"
            android:visibility="invisible" />

        <ImageButton
            android:id="@+id/nav_notifications"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@drawable/ic_notificacion1"
            android:padding="10dp"
            android:scaleType="centerInside"
            android:clickable="true"
            android:focusable="true"
            app:tint="@color/nav_icon_dark_inactive" />
    </LinearLayout>

    <!-- SETTINGS -->
    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:layout_weight="1"
        android:orientation="vertical"
        android:gravity="center_horizontal">

        <View
            android:id="@+id/indicator_settings"
            android:layout_width="24dp"
            android:layout_height="3dp"
            android:layout_marginBottom="4dp"
            android:background="@drawable/navbar_indicator"
            android:visibility="invisible" />

        <ImageButton
            android:id="@+id/nav_settings"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@drawable/ic_config"
            android:padding="10dp"
            android:scaleType="centerInside"
            android:clickable="true"
            android:focusable="true"
            app:tint="@color/nav_icon_dark_inactive" />
    </LinearLayout>

</LinearLayout>
