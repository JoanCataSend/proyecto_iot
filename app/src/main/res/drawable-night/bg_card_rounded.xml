<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Blanco semitransparente -->
    <solid android:color="@color/pill_darkmode"/>

    <!-- Bordes suaves -->
    <corners android:radius="28dp"/>

    <!-- Sombra simulada -->
    <stroke
        android:width="1dp"
        android:color="#14FFFFFF" /> <!-- sutil halo blanco -->

    <padding
        android:left="32dp"
        android:top="32dp"
        android:right="32dp"
        android:bottom="32dp"/>
</shape>
