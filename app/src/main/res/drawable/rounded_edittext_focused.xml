<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Fondo -->
<!--    <solid android:color="#FFFFFF" />-->

    <!-- Bordes redondeados -->
    <corners android:radius="8dp" />

    <!-- Borde azul (cuando se enfoca) -->
    <stroke
        android:width="1.5dp"
        android:color="@color/icono_normal" />

    <padding
        android:left="12dp"
        android:top="10dp"
        android:right="12dp"
        android:bottom="10dp" />
</shape>
